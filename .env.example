# Copy to ".env". The bot runs WITHOUT a key (BM25 retrieval + extractive
# answers). The key enables natural, grounded answer wording.

# Free API key from Google AI Studio: https://aistudio.google.com/apikey
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash
PORT=3002
